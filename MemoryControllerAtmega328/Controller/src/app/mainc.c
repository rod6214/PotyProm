#include <avr/io.h>
#include <util/delay.h>
// extern void entry_point();

int main() {
    // PORTD &= ~(1 << PIND0);
    // DDRD |= (1 << PIND0); 

    // while(1) {
    //     if (PORTD & (1 << PIND0))
    //         PORTD &= ~(1 << PIND0);
    //     else
    //         PORTD |= (1 << PIND0);
    //     _delay_ms(500);
    // }
    // entry_point();
    return 0;
}
