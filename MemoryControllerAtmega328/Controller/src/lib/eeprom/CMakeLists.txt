# include_directories(${I2C_DIR}/include)
# link_directories(${I2C_DIR}/lib)

# add_avr_library(
#     eeprom
#     eeprom.c
# )

# avr_target_link_libraries(eeprom i2c)
# avr_target_include_directories(eeprom PUBLIC i2c)
